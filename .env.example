DATABASE_URL=mysql+pymysql://username:password@host:port/dbname
SECRET_KEY=your-secret-key

#steps to set up the .env file
#1. Copy the .env.example file and rename it to .env
#2. Replace the placeholder values with your actual database credentials and secret key
#3. Ensure that the .env file is included in your .gitignore to prevent it from being committed to version control